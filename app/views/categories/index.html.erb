<div class="category__shops">
  <% @categories.each do |category| %>
    <%= link_to category.name, shop_path(category) %>
  <% end %>
  <% @category.shop_categories.each do |category| %>
    <%= link_to category.name, shop_path(category) %>
  <% end %>
</div>